import React from 'react';
// import PropTypes from 'prop-types';

import './styles.scss';

function App() {
  return (
    <div className="app">
      <h1>Bonjour</h1>
    </div>
  );
}


App.propTypes = {
  
};

export default React.memo(App);
